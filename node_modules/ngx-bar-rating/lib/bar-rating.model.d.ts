import { InjectionToken, Provider } from '@angular/core';
export interface BarRatingOptions {
    theme?: string;
    maxValue?: number;
    showText?: boolean;
    readonly?: boolean;
}
export declare const BAR_RATING_OPTIONS: InjectionToken<BarRatingOptions>;
export declare function provideBarRatingOptions(options: BarRatingOptions): Provider;
